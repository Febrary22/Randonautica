{
  "name": "escapade-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "일탈 (Escapade) — web prototype of the core adventure loop (coordinate generation with safety filtering, proximity beep, GPS path tracking).",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js",
    "generate-icons": "node scripts/generate-icons.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "pngjs": "^7.0.0"
  }
}
